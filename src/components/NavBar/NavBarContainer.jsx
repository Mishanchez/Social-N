import React from 'react';
import NavBar from './NavBar';


const NavBarContainer = (props) => {

    return <NavBar navBarFriendsData={props.navBarFriendsData} />
};

export default NavBarContainer;
