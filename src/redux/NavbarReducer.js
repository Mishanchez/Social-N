

let initialState = {
    navBarFriendsData: [
        { id: 1, name: 'Jimmy' },
        { id: 2, name: 'Alex' },
        { id: 5, name: 'Sarah' },
    ],

};

const navbarReducer=(state=initialState,action)=>{
return state
}

export default navbarReducer;